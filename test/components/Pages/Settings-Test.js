'use strict';
import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';

import Settings from '../../../js/components/Pages/Settings';

describe('page Settings', function() {});
